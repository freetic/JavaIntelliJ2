
public class OpDemo {
	public static void main(String[] args) {
		//int age = 24;
		//age = age + 1;
		//age++;
		//++age;
		//int result = ++age;    //1)먼저 1더하고, 나중에 result에 넣는다.
		//int result = age++;    //1)먼저 result에 넣고, 나중에 더한다.
		//System.out.println("age = " + ++age);
		//System.out.println("age = " + ++age++);
		//int age = -24;
		//System.out.println("age = " + Integer.toBinaryString(age));
		//int result = age << 12;    // -24 * 2^12
		//System.out.println("result = " + Integer.toBinaryString(result));
		//System.out.println("result = " + result);
		//int result = age >>> 4;   //-24 / 2^4
		//System.out.println("result = " + Integer.toBinaryString(result));
		//System.out.println("result = " + result);
		//int age = 23;
		//System.out.println((age % 2 == 0) ? "짝수" : "홀수");
//		if (age % 2 == 0) {
//			System.out.println("짝수");
//		}else	{
//			System.out.println("홀수");
//		}
		/*
		 * double avg = 89.5; char grade = (90 <= avg && avg <= 100) ? 'A' : (80 <= avg
		 * && avg < 90) ? 'B' : (70 <= avg && avg < 80) ? 'C' : (60 <= avg && avg < 70)
		 * ? 'D' : 'F';
		 */
		int age = 24;
		//++age;
		age = age + 3;
		age += 3;
		
		
		
		
		
		
	}
}
